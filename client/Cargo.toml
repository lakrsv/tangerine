[package]
name = "novel_shell_client"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aes-gcm = "0.10.1"

[build-dependencies]
uuid = { version = "1.3.0", features = ["v4"]}
